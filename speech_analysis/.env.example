# 이 파일을 복사해 .env 로 만들고 실제 키를 채우세요.
#   복사:  copy .env.example .env   (Windows)
# .env 는 .gitignore 에 의해 깃에 올라가지 않습니다.

# OpenAI API 키 (화행 적절성 LLM 채점에 사용)
OPENAI_API_KEY=sk-여기에-실제-키

# (선택) STT 백엔드. 현재는 local(Whisper)만 구현됨.
# STT_BACKEND=local
